<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mobilesensing.MainActivity" >

   <ListView
       android:id="@+id/myList"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_marginBottom="50dp" >

   </ListView>

   <EditText
       android:id="@+id/defaultNumber"
       android:layout_width="150dp"
       android:layout_height="wrap_content"
       android:layout_alignLeft="@+id/myList"
       android:layout_alignTop="@+id/occurs"
       android:layout_marginLeft="18dp"
       android:ems="10"
       android:hint="@string/defaultNumber"
       android:inputType="number" />

   <EditText
       android:id="@+id/occurs"
       android:layout_width="150dp"
       android:layout_height="wrap_content"
       android:layout_alignParentBottom="true"
       android:layout_marginBottom="0dp"
       android:layout_marginLeft="18dp"
       android:layout_toRightOf="@+id/defaultNumber"
       android:ems="10"
       android:hint="@string/occurs"
       android:inputType="number" />

</RelativeLayout>

